/* 
1) reformat the JSON
2) add one dropdown on the page
3) test out the apply button with one filter and display the results

*/
function filter() {

    // for loop to check each resturant object within the places array

        // create conditionals to check for filters that were selected

        // if (currentRest.borough == "Bronx" && currentRest.price == "$$"){
            // use document.createElement() to add resturnats to the page results div
        //}

       

}


apply.button.addEventListener("click", filter);